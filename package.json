{
  "name": "aws-all-service-actions",
  "version": "0.0.3",
  "description": "A very simple way to query for all valid actions for a specified AWS service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon ./index"
  },
  "author": "Julio Henrique",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.706.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.4"
  }
}
